# ============================================
# 블로그 오토 BYOK — 환경 변수 (선택사항)
# ============================================
# API 키는 브라우저 설정(⚙️)에서도 입력 가능합니다.
# 여기에 입력하면 서버 fallback으로 사용됩니다.
#
# 이 파일을 .env.local 로 복사:
#   cp .env.example .env.local

# ── 네이버 검색광고 API (키워드 조회용) ──
# 발급: https://searchad.naver.com → 도구 → API 사용 관리
# 브라우저에서도 입력 가능
NAVER_CUSTOMER_ID=
NAVER_API_KEY=
NAVER_SECRET_KEY=

# ── Google Gemini API (글쓰기 + 이미지) ──
# 발급: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# ── Claude API (선택) ──
# 발급: https://console.anthropic.com
CLAUDE_API_KEY=

# ── OpenAI GPT API (선택) ──
# 발급: https://platform.openai.com/api-keys
OPENAI_API_KEY=
